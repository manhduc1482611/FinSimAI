[project]
name = "finsim-math-engine"
version = "0.1.0"
description = "FinSimAI Math Engine — deterministic financial simulation core"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26.0",
]

[tool.ruff]
line-length = 100
