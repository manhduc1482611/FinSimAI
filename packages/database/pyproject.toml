[project]
name = "finsim-database"
version = "0.1.0"
description = "FinSimAI Database package — schema, migrations & seeds"
requires-python = ">=3.11"
dependencies = [
    "sqlalchemy[asyncio]>=2.0.0",
    "asyncpg>=0.29.0",
    "alembic>=1.13.0",
    "pyyaml>=6.0",
]
